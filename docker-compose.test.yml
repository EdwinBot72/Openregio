# Override: TEST-straat
# App alleen op localhost:5001 — uitsluitend bereikbaar via Caddy (HTTPS).
# DB op 127.0.0.1:5433 (niet naar buiten geëxposeerd).
services:
  app:
    ports:
      - "127.0.0.1:5001:5000"
  db:
    ports:
      - "127.0.0.1:5433:5432"
