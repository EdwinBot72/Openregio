# Override: ACCEPTATIE-straat
# App alleen op localhost:5002 — uitsluitend bereikbaar via Caddy (HTTPS).
# DB op 127.0.0.1:5434 (niet naar buiten geëxposeerd).
services:
  app:
    ports:
      - "127.0.0.1:5002:5000"
  db:
    ports:
      - "127.0.0.1:5434:5432"
