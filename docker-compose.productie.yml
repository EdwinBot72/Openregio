# Override: PRODUCTIE-straat
# App op host-poort 5000 (zet hier een reverse proxy / TLS voor).
# DB wordt NIET naar de host geëxposeerd (alleen intern bereikbaar).
services:
  app:
    restart: always
    ports:
      - "5000:5000"
  db:
    restart: always
