export default function OpenRegioDashboardLayout() {
  const topNav = ["Home", "Diensten", "Mijn portaal", "Contact"];
  const subNav = ["Dashboard", "Mijn zaken", "Brieven", "Regels", "Deadlines", "Updates"];

  const cards = [
    { size: "large" },
    { size: "large" },
    { size: "large" },
    { size: "large" },
    { size: "large" },
    { size: "small" },
  ];

  return (
    <div className="min-h-screen bg-[#f7f4ea] text-slate-800">
      <header className="border-b border-slate-200 bg-white/95 backdrop-blur">
        <div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5">
          <div className="flex items-center gap-3">
            <div className="h-10 w-10 rounded-2xl border border-slate-300 bg-slate-50" />
            <div className="space-y-1">
              <div className="h-3 w-28 rounded bg-slate-200" />
              <div className="h-2 w-20 rounded bg-slate-100" />
            </div>
          </div>

          <nav className="hidden items-center gap-8 md:flex">
            {topNav.map((item) => (
              <a
                key={item}
                href="#"
                className={`text-[15px] font-medium transition ${
                  item === "Mijn portaal" ? "text-[#0f6a67]" : "text-slate-600"
                }`}
              >
                {item}
              </a>
            ))}
          </nav>

          <div className="flex items-center gap-4">
            <div className="h-9 w-9 rounded-full border border-slate-200 bg-slate-50" />
            <div className="hidden h-9 w-9 rounded-full bg-[#e8eef0] md:block" />
            <div className="hidden md:block">
              <div className="h-3 w-14 rounded bg-slate-200" />
            </div>
          </div>
        </div>

        <div className="border-t border-slate-100 bg-white">
          <div className="mx-auto flex max-w-7xl items-center gap-8 overflow-x-auto px-6 py-4">
            {subNav.map((item) => (
              <button
                key={item}
                className={`relative whitespace-nowrap text-[15px] font-medium ${
                  item === "Dashboard" ? "text-slate-900" : "text-slate-500"
                }`}
              >
                {item}
                {item === "Dashboard" && (
                  <span className="absolute -bottom-4 left-0 h-[3px] w-full rounded-full bg-[#0f6a67]" />
                )}
              </button>
            ))}
          </div>
        </div>
      </header>

      <main className="mx-auto max-w-7xl px-6 py-12">
        <section className="mb-8">
          <div className="mb-3 h-10 w-56 rounded bg-[#d9d6cc]" />
          <div className="h-4 w-80 max-w-full rounded bg-[#e5e1d6]" />
        </section>

        <section className="mb-8 rounded-[28px] border border-[#e4dfd2] bg-[#f2eee3] p-3 shadow-sm">
          <div className="grid grid-cols-1 gap-3 lg:grid-cols-[190px_220px_1fr_1.3fr]">
            <div className="h-14 rounded-2xl border border-[#d9d4c6] bg-white" />
            <div className="h-14 rounded-2xl border border-[#d9d4c6] bg-white" />
            <div className="h-14 rounded-2xl border border-[#d9d4c6] bg-white" />
            <div className="h-14 rounded-2xl border border-[#d9d4c6] bg-white" />
          </div>
        </section>

        <section className="mb-8 grid gap-4 md:grid-cols-3">
          {[1, 2, 3].map((item) => (
            <div
              key={item}
              className="rounded-[28px] border border-[#ddd7ca] bg-white p-5 shadow-sm"
            >
              <div className="mb-4 flex items-center justify-between">
                <div className="h-4 w-24 rounded bg-slate-200" />
                <div className="h-9 w-9 rounded-2xl bg-slate-100" />
              </div>
              <div className="mb-3 h-8 w-16 rounded bg-slate-100" />
              <div className="h-3 w-28 rounded bg-slate-100" />
            </div>
          ))}
        </section>

        <section className="grid gap-5 lg:grid-cols-3">
          <div className="space-y-5 lg:col-span-2">
            <div className="grid gap-5 md:grid-cols-2">
              {cards.slice(0, 4).map((card, index) => (
                <CardBlock key={index} compact={false} />
              ))}
            </div>

            <div className="grid gap-5 md:grid-cols-[1.2fr_0.8fr]">
              <CardBlock compact={false} />
              <CardBlock compact />
            </div>
          </div>

          <div className="space-y-5">
            <CardBlock compact={false} tall />
            <CardBlock compact />
          </div>
        </section>
      </main>
    </div>
  );
}

function CardBlock({ compact = false, tall = false }: { compact?: boolean; tall?: boolean }) {
  return (
    <div
      className={`rounded-[28px] border border-[#ddd7ca] bg-white p-6 shadow-sm ${
        tall ? "min-h-[380px]" : compact ? "min-h-[190px]" : "min-h-[250px]"
      }`}
    >
      <div className="mb-5 flex items-start justify-between gap-4">
        <div className="space-y-3">
          <div className="h-5 w-40 rounded bg-slate-200" />
          <div className="h-4 w-28 rounded bg-slate-100" />
        </div>
        <div className="h-10 w-10 rounded-2xl bg-slate-100" />
      </div>

      <div className="mb-5 h-px w-full bg-slate-100" />

      <div className="space-y-4">
        {[1, 2, 3].map((item) => (
          <div key={item} className="flex items-center justify-between gap-4">
            <div className="flex flex-1 items-center gap-3">
              <div className="h-2.5 w-2.5 rounded-full bg-[#0f6a67]" />
              <div className="h-4 flex-1 rounded bg-slate-100" />
            </div>
            <div className="h-8 w-24 rounded-2xl bg-[#f2f4f5]" />
          </div>
        ))}
      </div>

      <div className="mt-6 h-px w-full bg-slate-100" />

      <div className="mt-5 flex items-center justify-between gap-4">
        <div className="h-4 w-28 rounded bg-slate-100" />
        <div className="h-4 w-24 rounded bg-slate-100" />
      </div>
    </div>
  );
}
