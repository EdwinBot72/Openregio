# Override: PRODUCTIE-straat
# App alleen op localhost:5000 — uitsluitend bereikbaar via Caddy (HTTPS).
# DB wordt NIET naar de host geëxposeerd (alleen intern bereikbaar).
services:
  app:
    restart: always
    ports:
      - "127.0.0.1:5000:5000"
  db:
    restart: always
